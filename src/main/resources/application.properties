# Database variables
#db.username=postgres
#db.password=root
#db.driver=org.postgresql.Driver
#db.url=jdbc:postgresql://localhost:5432/recruitment

db.username=root
db.password=root
db.driver=com.mysql.jdbc.Driver
db.url=jdbc:mysql://localhost/gst

# Application secret used for salting
app.secret=ThisIsASecretSoChangeMe

mail.host=smtp.gmail.com
mail.port=587
mail.username=fromMailId@gmail.com
mail.password=password

mail.subject=Email Verification from OJAS GSP Protal
mail.starttls.enable=true
mail.auth=true

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

